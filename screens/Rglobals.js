import React, { useState } from 'react';

const Rglobals = () => {
  const [name, setName] = useState('nothing');
  const [recipe, setRecipe] = useState('nothing');
  const [photo, setPhoto] = useState('nothing');

  // Your React component code goes here

  return (
    // JSX code for your component goes here
  );
};

export default Rglobals;
